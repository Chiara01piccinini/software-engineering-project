package org.example.app;

import org.example.app.controls.GestorePubblicazioni;
import org.example.app.model.*;
import org.example.app.model.ProdottoBase;
import org.example.app.view.EmailSystem;

public class Main {
    public static void main(String[] args) {
        /*
        //  Setup oggetti di sistema
        EmailSystem emailSystem = new EmailSystem();
        Curatore curatore = new Curatore("pinco","pallo",1234,"pincopallino@gmail.com");
        Prodotto prodotto = new Prodotto(1, "Olio Extra Vergine");
        GestorePubblicazioni gestore = new GestorePubblicazioni( curatore, emailSystem,prodotto);
        //  Venditore  invia richiesta
        Venditore produttore = new Produttore("ciccio","pasticcio",12345,"produttore@example.com",new Azienda());
        IFileInformazioni infoTestuale = new FileInformazioniTestuale("Nuova descrizione sull'olio extravergine.");
        produttore.inviaFile(gestore,infoTestuale);
        //  Output finale prodotto
        System.out.println("\nProdotto aggiornato:");
        System.out.println("ID: " + prodotto.getId());
        System.out.println("Nome: " + prodotto.getNome());
        System.out.println("Descrizione: " + prodotto.getDescrizione().getContenuto());
        */
    }
}
